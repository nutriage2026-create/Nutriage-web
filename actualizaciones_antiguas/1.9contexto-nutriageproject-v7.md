# CONTEXTO DEL PROYECTO — NutriAge
> Pega este texto al inicio de cualquier chat nuevo para que Claude entienda el proyecto completo sin explicar desde cero.

---

## ¿Qué es este proyecto?
Software SaaS llamado **NutriAge** para automatizar la gestión de clientes de nutricionistas. La nutricionista paga una membresía mensual para usar el sistema. El archivo principal actualmente se llama **`nutri-v7.html`** y contiene TODO el sistema en un solo archivo.

---

## Cliente / Nutricionista
- **Nombre:** Fernanda Ugarte
- **RUT:** 20.726.694-9
- **Banco:** Banco Estado
- **Tipo cuenta:** Cuenta RUT / Vista
- **Correo:** nutriage2026@gmail.com
- **Valor consulta:** $15.000 CLP
- **Contraseña panel:** fernanda2026

---

## Accesos del sistema
| Panel | Contraseña |
|---|---|
| Nutricionista | fernanda2026 |
| Administrador | admin2026 |

---

## Stack tecnológico
- **100% HTML + CSS + JS** en un solo archivo (sin frameworks, sin backend)
- **localStorage** como base de datos del navegador (clave: `nutriageV3`)
- **Jitsi Meet** integrado vía `external_api.js` para videollamadas (gratuito, sin instalar nada)
- **Sin servidor, sin base de datos externa** — todo corre en el navegador
- ⚠️ Jitsi solo funciona desde servidor web (no doble clic). Usar **VS Code Live Server** o subir a **Netlify**

---

## Arquitectura del sistema — 3 vistas

### 1. Vista Paciente (pública) — DUAL SUBSISTEMA
La vista paciente tiene **dos pestañas** con tabs táctiles grandes (mobile-first):

**Pestaña A — "📅 Nueva reserva"** (flujo de 4 pasos):
1. **Calendario** → elige fecha y hora disponible
2. **Formulario** → 33 campos clínicos
3. **Confirmación** → resumen de reserva
4. **Pago** → transferencia bancaria + subir comprobante
- Al finalizar se genera un **número de ticket único** (ej: `NA-20260409-001`) que el paciente debe guardar
- En la pantalla de éxito aparece un **botón verde de WhatsApp** para que el paciente notifique a Fernanda directamente con un mensaje pre-escrito (nombre, fecha, hora y ticket). Al hacer clic se marca `waNotificado: true` en localStorage y aparece badge de confirmación.

**Pestaña B — "🎟️ Ya tengo ticket"**:
- Campo de texto grande para ingresar el número de ticket
- Si el ticket existe y la cita es HOY → muestra botón verde "Unirme a la videollamada"
- Si la cita es otro día → mensaje informativo con la fecha
- Si el ticket no existe → mensaje de error

### 2. Vista Nutricionista (contraseña: fernanda2026)

#### Layout con Sidebar izquierdo (agregado en v7)
El panel nutricionista tiene una estructura de **dos columnas**:
- **Sidebar izquierdo** (`nutri-sidebar`, sticky en desktop): contiene el perfil y Mi Cuenta
- **Contenido principal** (`nutri-main`): citas, calendario, registros y estadísticas
- En mobile (`max-width: 800px`) el sidebar pasa arriba del contenido (columna única)

#### Sidebar — Tarjeta de Perfil
- Avatar con iniciales generadas dinámicamente desde el nombre guardado
- Nombre completo y rol ("Nutricionista · NutriAge")
- Se actualiza automáticamente al guardar en Mi Cuenta

#### Sidebar — Tarjeta "Mi Cuenta"
Campos editables guardados en `db.nutriProfile` (localStorage):
- **Nombre completo** → actualiza el avatar, el saludo "Bienvenida, [nombre]" y la tarjeta de perfil
- **Celular WhatsApp** → formato `569XXXXXXXX` → es el número al que llegan los mensajes de WhatsApp de los pacientes
- **Correo electrónico**
- Botón **"💾 Guardar información"** → guarda en localStorage, muestra badge verde de confirmación por 3 segundos
- Información persiste aunque se cierre sesión o recargue la página

#### Sidebar — Cerrar sesión
- Botón "← Cerrar sesión" al final del sidebar (reemplaza el botón que estaba en el header)

#### Contenido principal
- **Citas del día**: nombre, hora, motivo, número de ticket badge, dos botones por cita:
  - `📹 Sala Virtual` → lanza Jitsi
  - `WhatsApp` (botón verde) → abre WhatsApp con mensaje pre-escrito al paciente incluyendo hora y link Jitsi de su sala
- **Calendario editable** de disponibilidad (activa/desactiva días y horarios)
- **Registros de pacientes** desplegables con ficha resumida y ticket visible
- **Ficha clínica completa** por paciente (modal con TODA la info)
- **Sección Estadísticas de Ingresos**:
  - 4 tarjetas: ingresos hoy, semana, mes, consultas semanales
  - Gráfico de barras SVG (sin librerías) — 7 días o 30 días con etiquetas rotadas
  - Fernanda NO sabe que el admin también tiene este gráfico

### 3. Vista Administrador (contraseña: admin2026)
- **Tarjetas de estadísticas**: total pacientes, consultas hoy, ingresos esperados, formularios con comprobante, nº nutricionistas, ingresos anuales estimados
- **Tabla de pacientes**: columnas → Ticket + Nombre + Email + Teléfono + Fecha + Hora + Motivo + Comprobante + **WA** (nueva en v7) + Detalle
  - Columna **WA**: muestra badge `📲 Notificó` si el paciente hizo clic en el botón de WhatsApp, o `—` si no
- **Botón "💾 Descargar Excel"**: genera CSV con BOM UTF-8
- **Ficha clínica completa** por paciente (modal)
- **Estadísticas — Fernanda Ugarte**: tabla sesiones e ingresos por período
- **Gráfico de torta/donut**: % de pacientes por nutricionista
- **Gráfico de barras de ingresos** por día: 7 días o 30 días

---

## Ecosistema WhatsApp (agregado en v7)

### Flujo Paciente → Fernanda
1. El paciente completa la reserva y llega a la pantalla de éxito (Paso 5)
2. Aparece un bloque verde con botón **"Enviar confirmación a Fernanda"**
3. Al hacer clic → abre WhatsApp con mensaje pre-escrito que incluye: nombre, fecha, hora y ticket
4. El mensaje llega al número guardado en `db.nutriProfile.celular`
5. Se marca `waNotificado: true` en el objeto paciente y aparece badge de confirmación

### Flujo Fernanda → Paciente
1. En "Citas de hoy" cada cita tiene un botón **WhatsApp** verde
2. Al hacer clic → abre WhatsApp con el número del paciente y un mensaje que incluye: nombre, hora y link directo a la sala Jitsi

### Funciones clave WhatsApp
| Función | Descripción |
|---|---|
| `getNutriWA()` | Lee `db.nutriProfile.celular` (o fallback hardcodeado) |
| `waLinkPaciente(nombre, fecha, hora, ticket)` | Genera link wa.me con mensaje del paciente a Fernanda |
| `waLinkNutri(nombre, hora, telefono, jitsiSeed)` | Genera link wa.me con mensaje de Fernanda al paciente |
| `clickWaP()` | Marca `waNotificado:true` y muestra badge de confirmación |

### Número de WhatsApp de Fernanda
El número **ya no es hardcodeado** en el código. El sistema lo lee siempre desde `db.nutriProfile.celular`. Fernanda lo configura en el Sidebar → Mi Cuenta → campo "Celular WhatsApp". Si no hay número guardado, el fallback es `56912345678`.

---

## Sistema de Tickets
- Formato: `NA-AAAAMMDD-NNN` → ej: `NA-20260409-001`
- Se genera automáticamente al finalizar la reserva (función `generarTicket(dateKey)`)
- Secuencial por día: el primer paciente del 9 de abril es `001`, el segundo `002`, etc.
- Se guarda como propiedad `ticket` en el objeto paciente en localStorage
- Aparece como badge morado en: citas del día (nutri), registros (nutri), tabla admin

---

## Sistema de Videollamadas (Jitsi Meet)
- Librería: `https://meet.jit.si/external_api.js` cargada en `<head>`
- Nombre de sala generado automáticamente: `NutriAge_` + teléfono limpio del paciente
- Fernanda entra con toolbar completo (micro, cámara, pantalla, chat, participantes)
- El paciente entra con toolbar reducido (micro, cámara, chat, colgar)
- El paciente entra con micro muteado por defecto
- Modal full-screen con header NutriAge (logo, nombre paciente, indicador "En llamada")
- Funciones clave: `iniciarLlamada(seed, nombre)`, `unirseComoP()`, `cerrarLlamada()`
- Config: `prejoinConfig:{enabled:false}` + `prejoinPageEnabled:false` para entrar directo
- Fallback de 3.5s para ocultar spinner si los eventos no disparan

---

## Campos del formulario (33 campos)
**Datos personales:** nombre, email, teléfono, edad, ocupación, peso (kg), talla (metros)

**Historial médico:** motivo de consulta, enfermedades diagnosticadas, lesiones recientes, antecedentes familiares, cirugías

**Estilo de vida:** convivencia/quién cocina, actividad física, horario laboral, horario de sueño

**Hábitos alimentarios:** alimentos que no le gustan, almuerzo frecuente, horarios de comida, alergias/intolerancias, suplementos

**Consumo:** alcohol, tabaco/drogas

**Digestión:** síntomas gastrointestinales

**Frecuencia semanal:** cereales, legumbres, pescado, fruta, verduras, dulces, lácteos

**Registro 24h:** descripción libre

---

## Calendario
- Disponibilidad por defecto: lunes a viernes, 09:00 a 21:00 (próximos 60 días)
- Fernanda puede abrir/cerrar cualquier día y hora desde su panel
- El calendario del paciente solo muestra días y horas que Fernanda habilitó
- Horas ya tomadas no aparecen disponibles
- `initAvail()` y `renderCal()` se llaman en `DOMContentLoaded` (corregido en v7)

---

## Estructura de datos en localStorage (`nutriageV3`)
```js
{
  patients: [ /* array de objetos paciente */ ],
  availability: { "YYYY-MM-DD": ["09:00","10:00",...] },
  nutriProfile: {
    nombre: "Fernanda Ugarte",
    celular: "56912345678",  // ← usado por WhatsApp
    correo: "nutriage2026@gmail.com"
  }
}
```

Cada objeto paciente incluye:
```js
{
  id, date, time, dateFormatted, registeredAt,
  consultations, status, nutricionista,
  comprobante, comprobanteNombre,
  ticket,          // "NA-AAAAMMDD-NNN"
  waNotificado,    // true si el paciente hizo clic en botón WhatsApp
  // + los 33 campos del formulario
}
```

---

## Diseño y estilo
- **Paleta:** morado profundo `#3d2459` + verde salvia `#4a8c54` + crema `#faf7f2`
- **Variables CSS:** `--p1` al `--p6` (morados), `--s1` al `--s5` (verdes), `--g1` al `--g5` (grises)
- **Tipografía:** Cormorant Garamond (títulos) + Nunito (texto)
- **Tono:** natural, alimentación saludable, amigable, femenino
- **Mobile-first:** bottom nav en celular (3 tabs), nav superior en desktop
- Sidebar nutricionista: sticky en desktop, columna única en mobile (`max-width: 800px`)
- Color WhatsApp oficial: `#25D366` con clases `.btn-wa` (compacto) y `.btn-wa-lg` (grande)

---

## Funciones JS clave
| Función | Descripción |
|---|---|
| `getDB()` / `saveDB(db)` | Lee/escribe localStorage (`nutriageV3`) |
| `dk(date)` | Convierte Date a string `YYYY-MM-DD` |
| `fmtMoney(n)` | Formatea número como `$15.000` CLP |
| `generarTicket(dateKey)` | Genera código `NA-AAAAMMDD-NNN` |
| `loginConTicket()` | Valida ticket e ingresa al subsistema B |
| `switchPatientMode(mode)` | Alterna entre `'nueva'` y `'ticket'` |
| `renderNutriAppts()` | Renderiza citas del día con tickets y botones Sala + WhatsApp |
| `renderNutriRecords()` | Renderiza lista de pacientes con fichas |
| `renderNutriStats()` | Calcula ingresos y dibuja gráfico de barras (nutri) |
| `renderAdmin()` | Renderiza toda la vista admin |
| `renderAdminPieChart()` | Dibuja gráfico donut de pacientes por nutricionista |
| `buildBarSvg(svgId, days, nutri)` | Motor SVG para gráficos de barras (7 o 30 días) |
| `exportarExcel()` | Genera CSV descargable con todos los pacientes |
| `iniciarLlamada(seed, nombre)` | Abre sala Jitsi desde panel nutricionista |
| `unirseComoP()` | Paciente se une a sala Jitsi |
| `cerrarLlamada()` | Cierra y limpia instancia Jitsi |
| `saveNutriProfile()` | Guarda nombre, celular y correo de la nutri en localStorage |
| `loadNutriProfile()` | Carga perfil al iniciar sesión y rellena los campos del sidebar |
| `updateSidebarUI(p)` | Actualiza avatar, nombre e iniciales del sidebar con el perfil |
| `getNutriWA()` | Retorna el celular guardado en `db.nutriProfile.celular` |
| `waLinkPaciente(...)` | Genera link WhatsApp paciente → Fernanda |
| `waLinkNutri(...)` | Genera link WhatsApp Fernanda → paciente (con link Jitsi) |
| `clickWaP()` | Marca waNotificado y muestra badge de confirmación |

---

## Estado actual del proyecto (v7)
- ✅ Sistema completo en un solo archivo HTML (~1.720 líneas)
- ✅ Formulario con 33 campos, datos en localStorage
- ✅ Calendario con disponibilidad editable por Fernanda
- ✅ Sistema de tickets automático (`NA-AAAAMMDD-NNN`)
- ✅ Vista paciente dual: "Nueva reserva" + "Ya tengo ticket"
- ✅ Videollamadas Jitsi integradas (sin instalar nada, gratis)
- ✅ Gráficos de ingresos semanales (SVG puro, 7 y 30 días)
- ✅ Gráfico de torta: % pacientes por nutricionista
- ✅ Descarga Excel (CSV con BOM, compatible Windows)
- ✅ Optimizado para celular: tabs táctiles 52px, step bar con scroll
- ✅ Ficha clínica completa en modales (nutricionista + admin)
- ✅ **[v7] Sidebar izquierdo** en panel nutricionista con perfil y Mi Cuenta
- ✅ **[v7] Mi Cuenta**: nombre, celular WhatsApp y correo editables con persistencia
- ✅ **[v7] WhatsApp Paciente → Fernanda**: botón en pantalla de éxito, número dinámico desde perfil
- ✅ **[v7] WhatsApp Fernanda → Paciente**: botón en cada cita del día con link Jitsi incluido
- ✅ **[v7] Columna "WA" en tabla admin**: muestra si el paciente notificó por WhatsApp
- ✅ **[v7] Bug fix**: `cerrarLlamada()` estaba truncada — reconstruida correctamente
- ✅ **[v7] Bug fix**: faltaba `DOMContentLoaded` con `initAvail()` y `renderCal()` — calendario ya carga correctamente
- ⏳ Pendiente: publicar en internet (Netlify recomendado — arrastrar el archivo)
- ⏳ Pendiente: dominio propio (nutriagehot.cl o similar, ~$10 USD/año)
- ⏳ Pendiente: agregar más nutricionistas al sistema
- ⏳ Pendiente: migrar localStorage a Supabase cuando escale

---

## Próximos pasos sugeridos
1. **Publicar en Netlify** → ir a [netlify.com/drop](https://app.netlify.com/drop) y arrastrar `nutri-v7.html` → URL pública en segundos
2. **Fernanda configura su celular** → entrar al panel → Sidebar → Mi Cuenta → ingresar número formato `569XXXXXXXX` → Guardar
3. **Conseguir dominio propio** → nutriagehot.cl o similar (~$10 USD/año en NIC Chile)
4. **Agregar más nutricionistas** → cuando haya 3+ clientes pagando membresía
5. **Migrar base de datos** → Supabase gratis cuando localStorage no sea suficiente
6. **Cobro de membresía** → Mercado Pago suscripción (~$15-20 USD/mes por nutricionista)
7. **Notificaciones automáticas** → considerar email automático al reservar como complemento

---

## Historial de versiones
| Versión | Cambios principales |
|---|---|
| v4 | Sistema base: 3 vistas, formulario 33 campos, calendario, localStorage |
| v5 | Gráficos SVG de ingresos, gráfico de torta admin, descarga Excel |
| v6 | Jitsi Meet integrado, sistema de tickets, vista paciente dual, mobile fixes, gráfico 30 días |
| v7 | Sidebar + Mi Cuenta (nombre, celular, correo), ecosistema WhatsApp bidireccional, columna WA en admin, fix calendario, fix cerrarLlamada |

---

## Cómo usar este contexto
Al inicio de un chat nuevo escribe:

```
Aquí está el contexto de mi proyecto:
[pega este documento]

Lo que necesito ahora es: [tu pregunta o tarea específica]
```

Adjunta también el archivo `nutri-v7.html` si necesitas modificar el código.

> ⚠️ **Nota importante para Claude:** El archivo activo es `nutri-v7.html`. Siempre validar sintaxis JS con `node -e "new Function(jsCode)"` antes de entregar. Verificar que el archivo no esté truncado (debe terminar con `</html>`). Usar `DOMContentLoaded` para todas las inicializaciones. Si hay cambios grandes, reconstruir con script Python para evitar truncamientos.
